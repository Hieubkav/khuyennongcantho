import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function AdminPricesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Prices</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">Sẽ triển khai nhập/ghi giá ở bước tiếp theo.</p>
      </CardContent>
    </Card>
  );
}

